# Solar System Explorer

Version améliorée du site avec :
- favicon/logo dans l’onglet du navigateur (`favicon.svg`) ;
- animation plus vivante des planètes ;
- `index.html` + `style45.css` conservés à la racine.

Pour Vercel, déployer le contenu de ce dossier (pas le ZIP lui-même).
